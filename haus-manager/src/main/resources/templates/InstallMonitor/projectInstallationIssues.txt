{#if !toAdd.isEmpty()}
**GitHub organizations assigned to your project in organization records but missing from your project configuration:**
{#for org in toAdd}
  - https://github.com/{org}
{/for}

Please add these to your `githubOrganizations` list in {projectConfigPath}, or create an issue in {homeRepo} to correct foundation records.

{/if}
{#if !toRemove.isEmpty()}
**GitHub organizations in your project configuration but not in organization records:**
{#for org in toRemove}
  - https://github.com/{org}
{/for}

If these are new organizations, please create an issue in {homeRepo} to update foundation records. Otherwise, remove them from your `githubOrganizations` list in {projectConfigPath}.

{/if}
{#if !notInstalled.isEmpty()}
**GitHub organizations where HausManager is not installed:**
{#for org in notInstalled}
  - https://github.com/{org}
{/for}

Please install HausManager in these organizations, or create an issue in {homeRepo} if you need assistance.

{/if}
