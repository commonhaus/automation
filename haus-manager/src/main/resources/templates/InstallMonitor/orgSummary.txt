{#for group in orgGroups}
## {group.projectName}

{#for org in group.organizations}
{#if org.isMissingFromOrgConfig}  - ❓ {org.ghOrgName} (not in organization records)
{#else if org.isInstalled && !org.hasConfigIssues}  - ✅ {org.ghOrgName}
{#else if org.isInstalled && org.hasConfigIssues}  - ⚠️ {org.ghOrgName} (configuration issues)
{#else}  - ❌ {org.ghOrgName} (not installed)
{/if}
{/for}

{/for}
{#if !unmapped.isEmpty()}
## Unmapped

The following organizations have HausManager installed but are not mapped to any project or organization config:

{#for org in unmapped}
  - ❓ {org}
{/for}

Consider adding these to the organization config or removing the installation if not needed.
{/if}
